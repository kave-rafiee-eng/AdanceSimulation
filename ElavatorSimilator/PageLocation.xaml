<Page x:Class="ElavatorSimilator.PageLocation"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ElavatorSimilator"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Location">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="20*"/>
            <RowDefinition Height="100*"/>
        </Grid.RowDefinitions>



        <Canvas x:Name="Elevator" Grid.Row="1" Height="400" Width="500"  Background="LightGray" HorizontalAlignment="Left">

            <Rectangle Width="90" Height="12.5" Fill="Red"
               Stroke="Black" StrokeThickness="1"
               Canvas.Left="55"
               Canvas.Top="{Binding Y}"/>

            <!-- <Border Width="100" Height="300" Background="White" BorderBrush="Black" BorderThickness="0"
            Canvas.Left="50" Canvas.Top="10">
                <StackPanel Orientation="Horizontal" >

                    <Border Width="50" Height="300" Background="White" BorderBrush="Black" BorderThickness="2" HorizontalAlignment="Right"
                        Canvas.Left="50" Canvas.Top="10">

                    </Border>
                    
                </StackPanel>
            </Border>-->


            <!--
            <Rectangle Width="100" Height="60" Fill="Red"
                Canvas.Left="{Binding Path=X , Mode=OneWay}" Canvas.Top="50"
                       >
            </Rectangle>

           
            <Rectangle Width="10" Height="10" Fill="RoyalBlue" Stroke="Black" StrokeThickness="2"
                >
                <Rectangle.RenderTransform>
                    <RotateTransform Angle="4"/>
                </Rectangle.RenderTransform>
                <Rectangle.Effect>
                    <DropShadowEffect ShadowDepth="5" Color="Black" BlurRadius="10"/>
                </Rectangle.Effect>

            </Rectangle>


            <Ellipse Width="60" Height="60" Fill="OrangeRed" Canvas.Left="200" Canvas.Top="60"
                 Name="MyCircle">
                <Ellipse.Triggers>
                    <EventTrigger RoutedEvent="Ellipse.Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(Canvas.Left)"
                                             From="200" To="350" Duration="0:0:2" AutoReverse="True" RepeatBehavior="Forever"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Ellipse.Triggers>
            </Ellipse>


            <Path Stroke="DarkGreen" StrokeThickness="3" Fill="LightGreen"
              Canvas.Left="80" Canvas.Top="180"
              Data="M 0,0 C 50,-60 100,60 150,0 Z"/>


            <TextBlock FontSize="20" FontWeight="Bold" Foreground="DarkRed"
                   Canvas.Left="120" Canvas.Top="250"
                   Text="WPF Graphics!" RenderTransformOrigin="0.5,0.5">
                <TextBlock.RenderTransform>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </TextBlock.RenderTransform>
                <TextBlock.Triggers>
                    <EventTrigger RoutedEvent="TextBlock.Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)"
                                             From="1" To="1.5" Duration="0:0:1.5" AutoReverse="True" RepeatBehavior="Forever"/>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)"
                                             From="1" To="1.5" Duration="0:0:1.5" AutoReverse="True" RepeatBehavior="Forever"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </TextBlock.Triggers>
            </TextBlock>
            -->
        </Canvas>

    </Grid>
</Page>
